#!/bin/bash

# TIP: Use npx to run tools from
# node_modules - if it doesn't
# exist there, it will handle
# it globally. Never install
# a global package again!

# Runs create-react-app and generates 'my-project'
npx create-react-app my-project

